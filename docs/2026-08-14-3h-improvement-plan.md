# 3h 提升作战计划（19:12 起，22:12 归位）

主人：3 小时内全面提升功能/体验/测试。

## 我要做的事（优先级降序）
1. **灵魂交互过期契约 + 时间轴测试**（今天两次血训的直接落地）
   - 写 `docs/soul-interaction-lifecycle.md` 把 UX 契约定下来
   - 前端 `renderMsg` interact 卡片渲染时判定过期态（时长阈值 + 已滚出窗口 + 灵魂离线）
   - 过期后：置灰 + "灵魂已经不记得了" 文案 + 点击不发新消息
   - `scripts/journey-soul-interact-lifecycle.js` 时间轴模拟测试 + 旧实现反证
   - 接入 ci-check
2. **健康巡检加孤儿卡片扫描**
   - `health-probe.py` 加 L6 层：扫近 24h 未被响应且已滚出窗口的 interact 消息
   - 报警到主人 IM
3. **失望路径 UX 巡查**
   - 全站枚举"用户会失望的时刻"（发消息失败、上传失败、房间禁言、被踢、离线、余额不足...）
   - 每一处必须有明确反馈（toast/文案/降级路径），不能沉默
4. **视觉断言纳入 ci-check**
   - `scripts/idcheck-visual.py` 加进 ci-check（可跳过 CDP 时降级为软告警）
5. **顺手提升**（发现即修，安全小步）
   - 3h 结束前给主人交付简报，含 before/after 证据

## 铁律（本轮全程）
- **契约在前，测试在中，实现在后**：不先拍板契约、不先写测试的实现一律不做
- **视觉/时效性 bug 只做能"抓红"的东西**：加断言必须先证明旧实现会被抓红，否则不算数
- **不擅自动主人未提交的斗地主改动**（`js/games/deck.js` 未提交，避开）
- **每一项落地必须有 before/after 证据 + 反证**
- **发现拿不准的产品选择**：直接选主人偏保守/温和的一版做，回来给主人看，愿意改再改
- **控制 ci-check 密度基线不破**

## 进度追踪
（下面每完成一步就打勾，中途汇报节奏 ~45 分钟）

- [x] 步骤 A：灵魂交互过期契约文档（明确当前仓库没有成语接龙协议）
- [ ] 步骤 B：前端 interact/game 过期渲染（[blocked] 缺少真实成语接龙消息协议，避免误伤即时互动）
- [x] 步骤 C：journey 时间轴测试 + 反证（12/12）
- [x] 步骤 D：ci-check 接入并全绿
- [ ] 步骤 E：孤儿卡片健康巡检（[blocked] 当前没有可识别的游戏邀请字段）
- [x] 步骤 F：失望路径 UX 巡查（完成静默 catch 扫描，未擅改大批既有业务路径）
- [ ] 步骤 G：idcheck-visual 接入 ci-check（[blocked] 需要 CDP/线上环境，不应让离线 CI 伪装视觉验证）
- [x] 步骤 H：顺手提升（补充真实边界与阻塞说明）
- [x] 步骤 I：阶段性汇报材料已备齐
