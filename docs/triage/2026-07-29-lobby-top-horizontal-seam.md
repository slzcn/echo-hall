# Echo Bug 诊断单：大厅标题上方水平明暗切割

## §1 现象（Reproducibility）

- **设备**：用户移动端宽屏截图；本地以同截图尺寸 1716×486 CSS px、DPR 2 复现
- **运行环境**：日间模式，大厅页
- **触发步骤**：
  1. 进入大厅日间模式。
  2. 观察「回声厅」标题正上方至官方频道之间的背景。
  3. 可见一条笔直的水平明暗接缝；用户红圈标注区域尤其明显。
- **预期结果**：顶部背景从系统栏到大厅内容连续、柔和，无可识别的水平切割。
- **实际结果**：`#edgeFade` 顶部渐隐在 120px 结束，透明度斜率从持续变化瞬间变为 0；叠加 `.bg-grid` 与 `.bg-orb` 后形成水平明暗接缝。
- **修前截图**：用户消息原图 `om_x100b69ae93c9f870dd10867a21876af`；本地基准 `/tmp/eh_base.png`。

## §2 稳定复现

- ✅ 用户截图可见静态接缝，不依赖动画瞬间。
- ✅ 本地 CDP 以同宽高、DPR 2、日间大厅连续复现。
- ✅ 通过增强对比图 `/tmp/eh_base_contrast.png` 可稳定看到顶部渐隐结束带。
- **说明**：这是静态 CSS 背景合成问题，不需要键盘运行时数值；视频不比静态截图增加信息。

## §3 单一根因假设（Single Hypothesis）

- **本次假设根因**：`#edgeFade` 的顶部线性渐变在 `transparent 120px` 处结束，渐变斜率突然归零，导致该水平位置与下层网格／光斑产生明暗导数断点。
- **证据链**：
  1. 用户确认切割是直线，排除 `.bg-grid` 椭圆 `mask-image` 的弧形边界。
  2. 代码中唯一覆盖全宽、在标题上方具有固定水平边界的背景层是 `#edgeFade`，顶部透明终点为 120px。
  3. CDP 单变量实验：只隐藏 `#edgeFade`，该接缝消失、网格直接延伸到顶部；只隐藏 `.bg-grid` 产生的是网格消失，不是同一变化。
  4. 将 `#edgeFade` 临时改成长距离渐隐后，增强对比图中 120px 的收口带消失。
- **证伪实验**：不改文件，仅通过 CDP 覆盖 `#edgeFade` 的 `display`／`background`，固定其他背景层不变并截图对比。
- **证伪结果**：✅ 假设未被证伪，继续修复。

## §4 修复方案（Fix Plan）

- **动到的文件**：`index.html`；本诊断单。
- **动到的模块/行号**：顶部背景 `#edgeFade`（约 L196-L202）。
- **方案**：把 0→120px 的短线性收口改为多段缓出渐隐：顶部继续保持实色，随后逐级降低不透明度，并把尾部拉长；最后一段以低透明度缓慢归零，避免斜率突变。底部渐隐不动。
- **影响范围**：所有平台的日间／夜间背景；只改变装饰层，不影响布局、点击、键盘、滚动和房间状态。
- **历史回归检查**：保留 `#edgeFade` 顶部实色起点，不撤销其消除系统栏黑边的原用途；不触碰 `#soundwave`、`moodAura`、`.bg-grid`。
- **回滚方案**：回退本次 CSS commit 即可。

## §5 回归矩阵

| 场景 | 桌面宽屏 CDP | 移动宽屏 DPR2 | 日间 | 深夜 |
|---|:-:|:-:|:-:|:-:|
| 大厅标题上方无水平接缝 | ✅ | ✅ | ✅ | ✅ |
| 顶部系统栏无黑边 | ✅ | ✅ | ✅ | ✅ |
| 网格／光斑仍有柔和层次 | ✅ | ✅ | ✅ | ✅ |
| 大厅滚动与交互 | ✅ | ✅ | ✅ | ✅ |

## §6 提交约束

- ✅ 本次只修一个静态背景接缝。
- ✅ 不改数据库、键盘、房间状态和动画逻辑。
- ✅ 修复后运行 `bash scripts/ci-check.sh`。

## §7 修后确认

- **修后截图**：`/tmp/eh_exact_new.png`；同尺寸旧版 `/tmp/eh_exact_old.png`；10×增强对比 `/tmp/eh_exact_new_contrast.png`。
- **验证结论**：同尺寸、同主题、同 DOM 下仅替换 `#edgeFade`；新版 120px 硬收口带消失，顶部实色仍保留，网格与光斑自然透出。
- **CI 结果**：`bash scripts/ci-check.sh` 全部通过。
- **回归矩阵通过率**：16 / 16。
- **合入 main 时间**：待填写。


---

## §8 追加修复：「回声厅」标题背后浅色块（2026-07-29 18:27）

- **现象**：seam1 修复后顶部渐隐拉到 230px 变得柔和，但残留副作用——顶部长距离实色带在浅色日间叠出一块偏白的色块，正好压在「回声厅」标题后面。
- **验证**：CDP 逐层 elementsFromPoint 扒过标题上下左右，**标题本身与容器均无背景**；色块来源是 `#edgeFade` 顶部长实色带。
- **两方案对比**（日间/夜间各截图）：
  - A：彻底删掉顶部实色 → 日间干净，但**夜间 HTML 底层浅青氛围光贴到系统栏形成新的顶部亮带**（回退）。
  - B：顶部仅 8px 实色 + 40px 内快速淡出到透明 → 日间标题背后色块消除，夜间仍贴合系统栏无亮带。
- **采用**：B 方案。
- **版本**：BUILD_VER 从 `20260729-bg-seam1` bump 到 `20260729-bg-seam2`；SW_VERSION 同步。


---

## §9 桌面宽屏复现 + 终解：日间/夜间拆分（2026-07-29 18:47）

- **现象**：seam2（顶部 8px 实色）在移动端预览已看不出，但用户 Mac 桌面宽屏下「回声厅」标题这一横条背景仍明显偏白 —— 顶部实色在浅色日间叠出白带。
- **CDP 复现**：桌面 1440×2DPR 逐层 elementsFromPoint 确认标题周围唯一背景层是 `html`（浅青 + 微弱径向渐变），无独立色块元素；白带 = `#edgeFade` 顶部实色。
- **根因定性**：日间浅底下「顶部任何实色带」都会在标题后叠出偏白横条；而夜间又需要顶部实色贴合深色系统条防亮带。单一渐变无法同时满足 → 必须按模式拆分。
- **终解**：`#edgeFade` 默认（日间）顶部全透明；`html:has(body.deep-night) #edgeFade` 单独保留 8px 实色 40px 淡出。
- **验证**：桌面日间白带消失、背景连续；桌面夜间顶部深色贴合、无浅青亮带。
- **版本**：BUILD_VER `20260729-bg-seam2` → `20260729-bg-seam3`；SW_VERSION 同步。
